# Agentic Bot frontend

React/Vite frontend for the Agentic Bot project.

## Run locally

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` if your FastAPI backend is not hosted at `http://localhost:8000`.

All backend placeholders live in `src/api.js`. Replace the mock implementations there with your FastAPI routes.
